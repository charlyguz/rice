# Un solo exec-once. rice-autostart arranca lo que esté instalado
# (barra, notificaciones, portapapeles, fondo, polkit, idle) y se salta
# en silencio lo que falte. Mira el script: es shell plano.
exec-once = ~/.local/bin/rice-autostart
